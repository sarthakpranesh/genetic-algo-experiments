const HEIGHT = window.innerWidth < 360 ? window.innerHeight : 600;
const WIDTH = window.innerWidth < 360 ? window.innerWidth : 900;
const TOTAL = 1000;
const FRAMER = Math.floor(WIDTH / 2 - WIDTH / 6);
let isUser = true;
