const HEIGHT = window.innerWidth < 400 ? window.innerHeight - 80 : 600;
const WIDTH = window.innerWidth < 400 ? window.innerWidth : 900;
const TOTAL = 1000;
const FRAMER = window.innerWidth < 400 ? Math.floor(WIDTH / 6) : Math.floor(WIDTH / 2 - WIDTH / 6);
